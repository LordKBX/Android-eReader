<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/sync_menu_download"
        android:title="@string/sync_btn_download"
        android:icon="@drawable/ic_download" app:showAsAction="ifRoom"
        />
    <item
        android:id="@+id/sync_menu_upload"
        android:title="@string/sync_btn_upload"
        android:icon="@drawable/ic_upload" app:showAsAction="ifRoom"
        />
    <item
        android:id="@+id/sync_menu_refresh"
        android:title="@string/sync_btn_refresh"
        android:icon="@drawable/ic_refresh" app:showAsAction="ifRoom"
        />
</menu>